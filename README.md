Connect two devices (PCs or Laptops) physically or virtual

Then u need to run server app:

``g++ -o server main.cpp``
``./server``

To connect client use ur own or use telnet, SSH, netcat, etc.

``telnet [ip-addr] [port]``

Example:
``telnet 192.168.1.1 8080``